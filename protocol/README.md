# STM32 底盘通信协议

此目录用于保存上位机与 STM32 之间的二进制通信协议。协议先于硬件实现：这样模拟驱动、上位机控制器和 STM32 固件可以并行开发，并共享同一份字段定义。

下一步将定义：

- 帧头、版本、命令字、序号、长度、负载和 CRC16；
- `SET_TWIST`、`E_STOP`、`CLEAR_FAULT`、`PING` 命令；
- 包含轮速、电压、故障位和最后命令年龄的状态帧；
- 严格的大小端、单位、范围与失败处理规则；
- 可在没有开发板的电脑上运行的协议单元测试。
